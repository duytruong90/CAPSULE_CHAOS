# Audio

`AudioManager` is the central, optional cue bus. Playback emits typed draw/result
and rarity charge/impact cues. Subscribers can attach future asset playback; mute
suppresses delivery and adapter errors cannot interrupt presentation. Actual sound
assets and live mute/recovery polish remain for later phases.
